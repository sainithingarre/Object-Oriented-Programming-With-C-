#include <iostream>  
using namespace std;  
int main()  
{  
   int a,b;
   cout<<"enter a:";
   cin >>a;
   cout<<"enter b:";
   cin >>b;
   if(a==b)
   cout<<"Valid user name";
   else
   cout<<"Invalid user name";
   return 0;
}
